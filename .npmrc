# @ngrx/signals (latest 21.x) still peer-depends on Angular ^21 while this project
# runs Angular 22. Its SignalStore is built on Angular's stable signal primitives,
# which are unchanged in v22, so it works in practice. Allow the install until NgRx
# publishes a v22 build, then this flag can be removed.
legacy-peer-deps=true
